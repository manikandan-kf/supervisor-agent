"""Shared governance primitives for the SDLC agent platform.

Security screens, guardrail rules, audit sinks and the runtime bounds that every
agent on the platform applies — packaged once so the supervisor and the worker
agents install the same tested copy. See README.md for the module map.
"""

__version__ = "0.2.0"
