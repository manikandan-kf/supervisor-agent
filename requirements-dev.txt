# Developer tooling. Not installed into the serving container — the deploy job
# installs requirements.txt and bakes the governance wheel into the model.
#
# Equivalent to `pip install -e libs/agent_governance -e ".[dev]"`; kept as a
# file so CI can install the runtime and the tooling in one step.

# The shared governance library, editable, so a change in libs/ is visible to
# the supervisor's tests without a rebuild.
-e libs/agent_governance

# Lint + security ruleset (see [tool.ruff] in pyproject.toml).
ruff==0.16.7

# Both test trees. Everything runs offline — no workspace, no network, no
# database — so `pytest` needs nothing configured first.
pytest==9.1.1

# Static types. Gated in CI on the shared library, reported on the supervisor
# package — see [tool.mypy] in pyproject.toml for why the two differ.
mypy==1.18.2
types-PyYAML==6.0.12.20250915

# Coverage, reported in CI. No threshold is enforced yet: the number has to be
# visible before it can be argued about.
pytest-cov==7.0.0
